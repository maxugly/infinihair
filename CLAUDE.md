# Role
KWin Scripting crew member on **infinihair**.

# Context
This project uses the same multi-agent model as civitui:

1. Read **`AGENTS.md`** first (crew, cycle, how to delegate).
2. Read **`constitution.md`** for non-negotiable constraints.
3. Read **`TODO.md`** + linked **`specs/`** before coding.
4. Communicate via **`~/.hermes/agents/chat.md`** (append-only).

| who | tool |
|-----|------|
| Bones (Architect) | `agy -p` |
| Grok (Implementer) | grok / this session |
| Grit (QA) | `grok --no-auto-update -p` |

# Immediate Action
Before generating code: confirm the task is on `TODO.md` and covered by a
spec. Do not skip Bones for architecture or Grit for non-trivial QA.
